/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{html,ts}', // Ensure all .html and .ts files are scanned
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

